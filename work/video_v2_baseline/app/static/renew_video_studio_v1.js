(()=>{'use strict';let chosen=null,project=null,job=null,timer=null;
const root=()=>document.getElementById('renewVideoStudio'),norm=v=>String(v||'').replace(/\s/g,'').toLocaleUpperCase('tr-TR');
function allVehicles(){const m=new Map(),sources=typeof MEDIA_SOURCES!=='undefined'?MEDIA_SOURCES:{};for(const source of ['stocks','sales','acquisitions'])for(const x of (sources[source]||[])){const p=norm(x.plate);if(!p)continue;const title=x.vehicle_info||`${x.brand||''} ${x.model||''} ${x.version||''}`.trim();if(!m.has(p)||source==='stocks')m.set(p,{...x,source,title})}return [...m.values()]}
function boot(){if(!root())return;root().innerHTML=`<div class="video-shell"><div class="video-panel"><span class="video-eyebrow">RENEW VIDEO</span><h3>Araç seç</h3><p>Seçili çalışma dönemindeki araçlar.</p><input id="videoSearch" class="video-search" placeholder="Plaka, marka, model ara…"><div id="videoVehicles" class="video-vehicles"></div></div><main id="videoWork" class="video-panel"><div class="video-empty"><div><b>Video projesi için araç seç</b><p>8 fotoğraf, kapak, metin ve ses tek ekranda hazırlanır.</p></div></div></main><div id="videoSide" class="video-panel video-side"><span class="video-eyebrow">ÜRETİM ÖZETİ</span><div class="video-summary"><div><small>Format</small><b>9:16 Dikey</b></div><div><small>Süre</small><b>≈ 27 sn</b></div><div><small>Paylaşım</small><b>Onay sonrası</b></div></div><p class="video-note">Bu aşama yalnızca videoyu üretir. Instagram/TikTok paylaşımı bağlı değildir.</p></div></div>`;document.getElementById('videoSearch').oninput=renderVehicles;renderVehicles()}
function renderVehicles(){const h=document.getElementById('videoVehicles');if(!h)return;const q=norm(document.getElementById('videoSearch').value);const rows=allVehicles().filter(x=>!q||norm(`${x.plate} ${x.title}`).includes(q));h.innerHTML=rows.map(x=>`<button class="video-vehicle ${chosen&&norm(chosen.plate)===norm(x.plate)?'active':''}" data-plate="${esc(x.plate)}"><span><b>${esc(x.plate)}</b><small>${esc(x.title||'Araç')} • ${x.photo_count||0}/25</small></span><strong>›</strong></button>`).join('')||'<p>Bu dönemde araç bulunamadı.</p>';h.querySelectorAll('button').forEach(b=>b.onclick=()=>select(b.dataset.plate))}
async function select(plate){chosen=allVehicles().find(x=>norm(x.plate)===norm(plate));renderVehicles();const w=document.getElementById('videoWork');w.innerHTML='<div class="video-empty">Proje yükleniyor…</div>';try{project=await api('/api/video-studio/'+encodeURIComponent(plate));renderProject()}catch(e){w.innerHTML=`<div class="error-box">${esc(e.message)}</div>`}}
function data(){return {slots:project.slots,cover_slot:project.cover_slot,title:document.getElementById('videoTitle').value,subtitle:document.getElementById('videoSubtitle').value,narration:document.getElementById('videoNarration').value,voice:document.getElementById('videoVoice').value}}
function renderProject(){const w=document.getElementById('videoWork'),sel=new Map(project.slots.map((n,i)=>[n,i+1]));w.innerHTML=`<div class="video-work"><div class="video-head"><div><span class="video-eyebrow">VİDEO PROJESİ</span><h2>${esc(project.plate)} · ${esc(project.title||'Araç')}</h2></div><span class="video-status ${project.approved?'approved':''}">${project.approved?'ONAYLANDI':project.video_ready?'ONAY BEKLİYOR':'TASLAK'}</span></div><div class="video-fields"><label>Kapak başlığı<input id="videoTitle" value="${esc(project.title)}"></label><label>Kapak detayı<input id="videoSubtitle" value="${esc(project.subtitle)}"></label><label>Ses<select id="videoVoice"><option value="tr-TR-AhmetNeural" ${project.voice.includes('Ahmet')?'selected':''}>Ahmet · Erkek</option><option value="tr-TR-EmelNeural" ${project.voice.includes('Emel')?'selected':''}>Emel · Kadın</option></select></label><label>Şablon<select><option>RENEW Editorial · 9:16</option></select></label></div><div><span class="video-eyebrow">8 FOTOĞRAF SEÇ · YILDIZA BASARAK KAPAK YAP</span><div class="video-slots">${project.photos.map(x=>`<button class="video-photo ${sel.has(x.slot)?'selected':''} ${project.cover_slot===x.slot?'cover':''}" data-slot="${x.slot}"><img src="${x.url}?v=${Date.now()}"><em>${sel.get(x.slot)||x.slot}</em>${sel.has(x.slot)?`<strong title="Kapak yap">☆</strong>`:''}</button>`).join('')}</div></div><div class="video-script"><label>Seslendirme metni<textarea id="videoNarration">${esc(project.narration)}</textarea></label></div><div id="videoProgressWrap" ${job?'':'hidden'}><p id="videoProgressText">Hazırlanıyor…</p><div class="video-progress"><i id="videoProgressBar"></i></div></div>${project.video_ready?`<video class="video-preview" controls playsinline src="/api/video-studio/${encodeURIComponent(project.plate)}/file?v=${Date.now()}"></video>`:''}<div class="video-actions"><button onclick="renewVideoSave()">Taslağı Kaydet</button><button class="primary" onclick="renewVideoGenerate()">▶ Videoyu Oluştur</button>${project.video_ready&&!project.approved?'<button class="success" onclick="renewVideoApprove()">✓ Videoyu Onayla</button>':''}</div></div>`;w.querySelectorAll('.video-photo').forEach(b=>b.onclick=e=>photoClick(Number(b.dataset.slot),e.target.closest('strong')!==null))}
function photoClick(slot,cover){if(cover){project.cover_slot=slot;renderProject();return}const i=project.slots.indexOf(slot);if(i>=0){project.slots.splice(i,1);if(project.cover_slot===slot)project.cover_slot=project.slots[0]||0}else if(project.slots.length<8){project.slots.push(slot);if(!project.cover_slot)project.cover_slot=slot}else alert('En fazla 8 fotoğraf seçebilirsiniz.');renderProject()}
async function save(){const r=await api(`/api/video-studio/${encodeURIComponent(project.plate)}/project`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data())});project={...project,...r.project};alert('Video taslağı kaydedildi.');renderProject()}
async function generate(){if(project.slots.length!==8){alert('Video için tam 8 fotoğraf seçin.');return}const w=document.getElementById('videoWork');w.classList.add('video-loading');try{const r=await api(`/api/video-studio/${encodeURIComponent(project.plate)}/generate`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data())});job=r.job_id;w.classList.remove('video-loading');poll()}catch(e){w.classList.remove('video-loading');alert(e.message)}}
async function poll(){clearTimeout(timer);try{const s=await api(`/api/video-studio/${encodeURIComponent(project.plate)}/status?job_id=${job}`);document.getElementById('videoProgressWrap').hidden=false;document.getElementById('videoProgressText').textContent=s.message;document.getElementById('videoProgressBar').style.width=(s.progress||0)+'%';if(s.state==='ready'){job=null;project=await api('/api/video-studio/'+encodeURIComponent(project.plate));renderProject();return}if(s.state==='error'){job=null;alert('Video üretilemedi: '+s.message);return}}catch(e){}timer=setTimeout(poll,1500)}
async function approve(){await api(`/api/video-studio/${encodeURIComponent(project.plate)}/approve`,{method:'POST'});project.approved=true;renderProject()}
window.renewVideoSave=save;window.renewVideoGenerate=generate;window.renewVideoApprove=approve;
document.addEventListener('DOMContentLoaded',()=>{boot();const nav=document.querySelector('.nav[data-page="video-studio"]');if(nav)nav.addEventListener('click',()=>setTimeout(()=>{boot();renderVehicles()},0));const m=document.getElementById('globalMonth');if(m)m.addEventListener('change',()=>{chosen=null;project=null;setTimeout(()=>{boot();renderVehicles()},300)});let tries=0;const ready=setInterval(()=>{tries++;if(allVehicles().length){clearInterval(ready);renderVehicles()}else if(tries>30)clearInterval(ready)},300)});
})();
