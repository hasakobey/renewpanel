/* Display-only enhancements; calculations remain in the existing application. */
(()=>{function init(){const card=document.getElementById('finansKartPanel');if(!card)return;const input=document.getElementById('tutar'),select=document.getElementById('taksit');const parse=s=>Number(String(s).replace(/[^0-9,.-]/g,'').replace(/\./g,'').replace(',','.'))||0;
const slider=document.createElement('input');slider.type='range';slider.id='kcAmountSlider';slider.min='0';slider.max='600000';slider.step='1';slider.setAttribute('aria-label','Tutarı kaydırarak seç');const labels=document.createElement('div');labels.className='kc-slider-labels';labels.innerHTML='<span>₺0</span><span></span>';input.after(slider,labels);
function sync(){const amount=parse(input.value);slider.max=String(Math.max(Number(slider.max),600000,Math.ceil(amount/100000)*100000));slider.value=amount;slider.style.setProperty('--fill',Math.max(0,Math.min(100,amount/Number(slider.max)*100))+'%');labels.lastElementChild.textContent='₺'+Number(slider.max).toLocaleString('tr-TR');slider.setAttribute('aria-valuetext',input.value+' Türk lirası')}
slider.addEventListener('input',()=>{input.value=Number(slider.value).toLocaleString('tr-TR');input.dispatchEvent(new Event('input',{bubbles:true}))});input.addEventListener('input',sync);sync();
const table=document.getElementById('rcCompareBody'),wrap=table.closest('.rc-table-wrap'),bars=document.createElement('div');bars.className='kc-bars';bars.setAttribute('aria-label','Taksit alternatifleri');wrap.hidden=true;wrap.after(bars);
function render(){sync();const rows=[...table.rows],values=rows.map(r=>parse(r.cells[3].textContent)),max=Math.max(1,...values);rows.forEach((row,i)=>{const term=parseInt(row.cells[0].textContent),b=bars.children[i]||document.createElement('button');const fresh=!b.parentElement;b.type='button';b.className='kc-bar-row';b.classList.toggle('kc-selected',term===Number(select.value));b.setAttribute('aria-pressed',String(term===Number(select.value)));if(fresh)b.innerHTML='<span class="kc-bar-opt"><b></b><small></small></span><span class="kc-track" aria-hidden="true"><span class="kc-fill"></span></span><span class="kc-bar-values"><small></small><b></b></span>';b.querySelector('.kc-bar-opt b').textContent=row.cells[0].textContent;b.querySelector('.kc-bar-opt small').textContent=row.cells[1].textContent;b.querySelector('.kc-bar-values small').textContent=row.cells[2].textContent+' / ay';b.querySelector('.kc-bar-values b').textContent=row.cells[3].textContent;const fill=b.querySelector('.kc-fill');fill.style.width=values[i]/max*100+'%';fill.style.animation='none';if(fresh)b.addEventListener('click',()=>{select.value=term;select.dispatchEvent(new Event('change',{bubbles:true}))});if(fresh)bars.append(b)})}
new MutationObserver(render).observe(table,{childList:true});render();
}if(document.readyState!=='complete')window.addEventListener('load',init);else init()})();
